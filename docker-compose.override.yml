# Local development overrides — automatically merged with docker-compose.yml
# This file is NOT used in CI/CD or production.

services:
  api:
    build:
      target: development
    environment:
      - DEBUG=true
    volumes:
      - .:/app

  mcp:
    volumes:
      - .:/app
